#include "FileReplacer.hpp"

FileReplacer::FileReplacer(const std::string& filename,
                           const Replacer& replacer)
    : filename_(filename), replacer_(replacer) {}

bool FileReplacer::Execute() const { std::str }
